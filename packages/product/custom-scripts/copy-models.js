#!/usr/bin/env node

// const fs = require('fs') // Import the filesystem module
// const path = require('path')

// fs.readFile('src/sdk/schema/Product.schema', 'utf8', readingFile)

// function readingFile(error, data) {
//   if (error) {
//     console.log(error)
//   } else {
//     console.log(data)

//     fs.appendFile(process.cwd() + '/apps/myshop/prisma/' + 'schema.prisma', data, function (err) {
//       if (err) throw err
//       console.log('Saved!')
//     })
//   }
// }
